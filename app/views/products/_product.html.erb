<%= link_to product_path(product), class: 'product' do %>
  <br><%= image_tag product.photo, width: 100 if product.photo.attached? %>
  <h2> <%= product.title %> </h2>    
  <span><%= product.price %>$</span><br><br>
<% end %>